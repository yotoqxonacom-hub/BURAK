{
  "name": "burak",
  "version": "1.0.0",
  "description": "This is the burak project backend",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --exec ts-node src/server.ts",
    "train": "nodemon --exec ts-node train.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "MIT",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.30.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  }
}